# Socket.IO 실시간 채팅 예제

Node.js의 Socket.IO를 사용한 간단한 실시간 채팅 애플리케이션입니다.

## 🚀 설치 및 실행

### 1. 의존성 설치
```bash
npm install
```

### 2. 서버 실행
```bash
npm start
# 또는
node server.js
```

### 3. 브라우저에서 접속
```
http://localhost:3000
```

## ✨ 주요 기능

- **실시간 채팅**: 여러 사용자가 동시에 채팅 가능
- **사용자 입장/퇴장 알림**: 누가 들어오고 나갔는지 실시간 알림
- **접속자 수 표시**: 현재 채팅방에 있는 사용자 수 실시간 표시
- **시간 표시**: 각 메시지에 전송 시간 표시
- **반응형 디자인**: 모바일과 데스크톱에서 모두 사용 가능

## 🛠️ 기술 스택

- **Backend**: Node.js, Socket.IO
- **Frontend**: HTML5, CSS3, JavaScript (Vanilla)
- **실시간 통신**: WebSocket (Socket.IO)

## 📱 사용 방법

1. 서버를 실행합니다
2. 브라우저에서 `http://localhost:3000`에 접속합니다
3. 사용자 이름을 입력합니다
4. 메시지를 입력하고 전송 버튼을 클릭하거나 Enter 키를 누릅니다
5. 여러 브라우저 탭을 열어서 여러 사용자로 테스트할 수 있습니다

## 🔧 Socket.IO 이벤트

### 클라이언트 → 서버
- `user-join`: 사용자 입장
- `chat-message`: 채팅 메시지 전송
- `user-leave`: 사용자 퇴장

### 서버 → 클라이언트
- `user-joined`: 다른 사용자 입장 알림
- `user-left`: 다른 사용자 퇴장 알림
- `chat-message`: 채팅 메시지 수신
- `user-count`: 접속자 수 업데이트
- `server-message`: 서버 메시지

## 🎯 학습 포인트

이 예제를 통해 다음을 학습할 수 있습니다:

1. **Socket.IO 기본 사용법**
2. **실시간 양방향 통신**
3. **이벤트 기반 프로그래밍**
4. **사용자 상태 관리**
5. **브로드캐스팅과 개별 메시지 전송**

## 🔍 코드 구조

- `server.js`: Socket.IO 서버와 HTTP 서버 설정
- `package.json`: 프로젝트 설정 및 의존성
- HTML/CSS/JavaScript: 클라이언트 코드 (서버 파일에 포함)

## 🚨 주의사항

- 이 예제는 학습용으로 제작되었습니다
- 프로덕션 환경에서는 추가적인 보안 및 에러 처리가 필요합니다
- 메시지는 서버에 저장되지 않으므로 서버 재시작 시 사라집니다

